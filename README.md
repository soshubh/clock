# Dot Matrix Clock

A fullscreen dot-matrix clock with customizable pitch, size, glow, accent color, and 12H/24H format. Settings are saved to localStorage.

Open `index.html` in a browser, or deploy to any static host.
